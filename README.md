# cloudflare-vless

VLESS on Cloudflare Workers.

## Deployment

1. Install node.
2. Modify entries in `wrangler.jsonc`.
3. Run following commands:

```bash
npm install
npm run deploy
```
